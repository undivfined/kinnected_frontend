import { View, Text } from 'react-native';

export default function AddConnection() {
    return (
        <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
          <Text>Add Connection Button</Text>
        </View>
      );
}