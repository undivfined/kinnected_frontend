import { View, Text } from 'react-native';

export default function DeleteConnection() {
    return (
        <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
          <Text>Delete Connection Button</Text>
        </View>
      );
}